--[=[
  🐈🐈😺🐈🐈😺🐈🐈😺🐈🐈😺🐈🐈
🐈╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╭╮╱😺╱🐈╱╱╱╱╭╮🐈
🐈╱╱🐈╱╱╱╱╱╱╱🐈╱╱🐈╱╱╱┃┃╱🐈╱╱┃┃🐈
🐈╭━━┳━━┳━┳━┳╮╱╭╮┃╰━╳🐈╮╭┫🐈━╮🐈
🐈┃╭╮┃╭╮┃╭┫╭┫┃🐈┃┃┃╭╮┃🐈┃┃╭╮┃🐈
🐈┃╰╯┃╭╮┃┃┃🐈┃╰━╯🐈┃┃┃┃🐈┃╰╯┃🐈
🐈╰━╮┣╯╰┻╯🐈╰━╮╭╯🐈╯╰┻━━┻━━╯🐈
🐈╭━╯🐈╱╱╱╱╱╱╭━╯🐈┃🐈
🐈╰━━╯🐈╱╱╱╱╱🐈╰━━╯🐈
  🐈🐈😺🐈🐈
]=]

local function createGui(parent)
	--ScreenGui1--
	local ScreenGui1 = Instance.new("ScreenGui",parent)
	ScreenGui1["ZIndexBehavior"] = Enum.ZIndexBehavior.Sibling
	----ScreenGui1----
	--Frame2--
	local Frame2 = Instance.new("Frame",ScreenGui1)
	Frame2["Position"] = UDim2.new(0.359325796, 0, 0.344601959, 0)
	Frame2["BorderColor3"] = Color3.new(0, 0, 0)
	Frame2["Size"] = UDim2.new(0, 638, 0, 350)
	Frame2["BorderSizePixel"] = 0
	Frame2["BackgroundColor3"] = Color3.new(0.0313726, 0.635294, 0.482353)
	----Frame2----
	--UICorner3--
	local UICorner3 = Instance.new("UICorner",Frame2)
	UICorner3["CornerRadius"] = UDim.new(0, 4)
	----UICorner3----
	--UIStroke4--
	local UIStroke4 = Instance.new("UIStroke",Frame2)
	UIStroke4["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke4["Thickness"] = 5
	----UIStroke4----
	--Folder5--
	local Folder5 = Instance.new("Folder",Frame2)
	----Folder5----
	--ImageLabel6--
	local ImageLabel6 = Instance.new("ImageLabel",Folder5)
	ImageLabel6["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel6["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel6["Name"] = "Squid"
	ImageLabel6["Image"] = "rbxassetid://9875928470"
	ImageLabel6["BackgroundTransparency"] = 1
	ImageLabel6["Position"] = UDim2.new(1.12439334, 0, -0.0488480069, 0)
	ImageLabel6["Size"] = UDim2.new(0, 41, 0, 32)
	ImageLabel6["ZIndex"] = 7
	ImageLabel6["BorderSizePixel"] = 0
	ImageLabel6["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel6----
	--Script7--
	local Script7 = Instance.new("Script",ImageLabel6)
	----Script7----
	--ImageLabel8--
	local ImageLabel8 = Instance.new("ImageLabel",Folder5)
	ImageLabel8["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel8["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel8["BackgroundTransparency"] = 1
	ImageLabel8["Rotation"] = 55
	ImageLabel8["Image"] = "rbxassetid://9875928470"
	ImageLabel8["Name"] = "Squid"
	ImageLabel8["Position"] = UDim2.new(0.928317845, 0, -0.219391301, 0)
	ImageLabel8["Size"] = UDim2.new(0, 87, 0, 70)
	ImageLabel8["ZIndex"] = 7
	ImageLabel8["BorderSizePixel"] = 0
	ImageLabel8["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel8----
	--Script9--
	local Script9 = Instance.new("Script",ImageLabel8)
	----Script9----
	--ImageLabel10--
	local ImageLabel10 = Instance.new("ImageLabel",Folder5)
	ImageLabel10["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel10["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel10["BackgroundTransparency"] = 1
	ImageLabel10["Rotation"] = 55
	ImageLabel10["Image"] = "rbxassetid://9875928470"
	ImageLabel10["Name"] = "Squid"
	ImageLabel10["Position"] = UDim2.new(0.975667357, 0, -0.000813249848, 0)
	ImageLabel10["Size"] = UDim2.new(0, 41, 0, 32)
	ImageLabel10["ZIndex"] = 7
	ImageLabel10["BorderSizePixel"] = 0
	ImageLabel10["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel10----
	--Script11--
	local Script11 = Instance.new("Script",ImageLabel10)
	----Script11----
	--ImageLabel12--
	local ImageLabel12 = Instance.new("ImageLabel",Folder5)
	ImageLabel12["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel12["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel12["BackgroundTransparency"] = 1
	ImageLabel12["Rotation"] = 55
	ImageLabel12["Image"] = "rbxassetid://9875928470"
	ImageLabel12["Name"] = "Squid"
	ImageLabel12["Position"] = UDim2.new(1.02567518, 0, 0.0251897313, 0)
	ImageLabel12["Size"] = UDim2.new(0, 75, 0, 62)
	ImageLabel12["ZIndex"] = 7
	ImageLabel12["BorderSizePixel"] = 0
	ImageLabel12["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel12----
	--Script13--
	local Script13 = Instance.new("Script",ImageLabel12)
	----Script13----
	--ImageLabel14--
	local ImageLabel14 = Instance.new("ImageLabel",Folder5)
	ImageLabel14["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel14["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel14["BackgroundTransparency"] = 1
	ImageLabel14["Rotation"] = 42
	ImageLabel14["Image"] = "rbxassetid://9875928470"
	ImageLabel14["Name"] = "Squid"
	ImageLabel14["Position"] = UDim2.new(1.17302823, 0, -0.217520833, 0)
	ImageLabel14["Size"] = UDim2.new(0, 57, 0, 50)
	ImageLabel14["ZIndex"] = 7
	ImageLabel14["BorderSizePixel"] = 0
	ImageLabel14["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel14----
	--Script15--
	local Script15 = Instance.new("Script",ImageLabel14)
	----Script15----
	--ImageLabel16--
	local ImageLabel16 = Instance.new("ImageLabel",Folder5)
	ImageLabel16["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel16["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel16["BackgroundTransparency"] = 1
	ImageLabel16["Rotation"] = 55
	ImageLabel16["Image"] = "rbxassetid://9875928470"
	ImageLabel16["Name"] = "Squid"
	ImageLabel16["Position"] = UDim2.new(1.06867075, 0, -0.0742464811, 0)
	ImageLabel16["Size"] = UDim2.new(0, 21, 0, 16)
	ImageLabel16["ZIndex"] = 7
	ImageLabel16["BorderSizePixel"] = 0
	ImageLabel16["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel16----
	--Script17--
	local Script17 = Instance.new("Script",ImageLabel16)
	----Script17----
	--ImageLabel18--
	local ImageLabel18 = Instance.new("ImageLabel",Folder5)
	ImageLabel18["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel18["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel18["BackgroundTransparency"] = 1
	ImageLabel18["Rotation"] = 55
	ImageLabel18["Image"] = "rbxassetid://9875928470"
	ImageLabel18["Name"] = "Squid"
	ImageLabel18["Position"] = UDim2.new(1.06772518, 0, -0.284068704, 0)
	ImageLabel18["Size"] = UDim2.new(0, 41, 0, 32)
	ImageLabel18["ZIndex"] = 7
	ImageLabel18["BorderSizePixel"] = 0
	ImageLabel18["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel18----
	--Script19--
	local Script19 = Instance.new("Script",ImageLabel18)
	----Script19----
	--ImageLabel20--
	local ImageLabel20 = Instance.new("ImageLabel",Folder5)
	ImageLabel20["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel20["BackgroundTransparency"] = 1
	ImageLabel20["Rotation"] = -170
	ImageLabel20["Image"] = "http://www.roblox.com/asset/?id=79036645393661"
	ImageLabel20["Name"] = "Squid"
	ImageLabel20["Position"] = UDim2.new(1.00535345, 0, -0.397725314, 0)
	ImageLabel20["Size"] = UDim2.new(0, 198, 0, 198)
	ImageLabel20["ZIndex"] = 7
	ImageLabel20["BorderSizePixel"] = 0
	ImageLabel20["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel20----
	--Script21--
	local Script21 = Instance.new("Script",ImageLabel20)
	----Script21----
	--Folder22--
	local Folder22 = Instance.new("Folder",Frame2)
	----Folder22----
	--ImageLabel23--
	local ImageLabel23 = Instance.new("ImageLabel",Folder22)
	ImageLabel23["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel23["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel23["BackgroundTransparency"] = 1
	ImageLabel23["Rotation"] = 55
	ImageLabel23["Image"] = "rbxassetid://9875928470"
	ImageLabel23["Name"] = "PufferFish"
	ImageLabel23["Position"] = UDim2.new(0.00597063499, 0, 0.767220616, 0)
	ImageLabel23["Size"] = UDim2.new(0, 38, 0, 31)
	ImageLabel23["ZIndex"] = 7
	ImageLabel23["BorderSizePixel"] = 0
	ImageLabel23["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel23----
	--Script24--
	local Script24 = Instance.new("Script",ImageLabel23)
	----Script24----
	--ImageLabel25--
	local ImageLabel25 = Instance.new("ImageLabel",Folder22)
	ImageLabel25["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel25["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel25["Name"] = "PufferFish"
	ImageLabel25["Image"] = "rbxassetid://9875928470"
	ImageLabel25["BackgroundTransparency"] = 1
	ImageLabel25["Position"] = UDim2.new(-0.0657246262, 0, 0.752558768, 0)
	ImageLabel25["Size"] = UDim2.new(0, 38, 0, 31)
	ImageLabel25["ZIndex"] = 7
	ImageLabel25["BorderSizePixel"] = 0
	ImageLabel25["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel25----
	--Script26--
	local Script26 = Instance.new("Script",ImageLabel25)
	----Script26----
	--ImageLabel27--
	local ImageLabel27 = Instance.new("ImageLabel",Folder22)
	ImageLabel27["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel27["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel27["BackgroundTransparency"] = 1
	ImageLabel27["Rotation"] = 55
	ImageLabel27["Image"] = "rbxassetid://9875928470"
	ImageLabel27["Name"] = "PufferFish"
	ImageLabel27["Position"] = UDim2.new(-0.0138678001, 0, 0.711879194, 0)
	ImageLabel27["Size"] = UDim2.new(0, 19, 0, 14)
	ImageLabel27["ZIndex"] = 7
	ImageLabel27["BorderSizePixel"] = 0
	ImageLabel27["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel27----
	--Script28--
	local Script28 = Instance.new("Script",ImageLabel27)
	----Script28----
	--ImageLabel29--
	local ImageLabel29 = Instance.new("ImageLabel",Folder22)
	ImageLabel29["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel29["Name"] = "PufferFish"
	ImageLabel29["Image"] = "http://www.roblox.com/asset/?id=71379009952076"
	ImageLabel29["BackgroundTransparency"] = 1
	ImageLabel29["Position"] = UDim2.new(-0.218581632, 0, 0.788288951, 0)
	ImageLabel29["Size"] = UDim2.new(0, 130, 0, 104)
	ImageLabel29["ZIndex"] = 7
	ImageLabel29["BorderSizePixel"] = 0
	ImageLabel29["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel29----
	--Script30--
	local Script30 = Instance.new("Script",ImageLabel29)
	----Script30----
	--ImageLabel31--
	local ImageLabel31 = Instance.new("ImageLabel",Folder22)
	ImageLabel31["ImageColor3"] = Color3.new(0.964706, 0.886275, 1)
	ImageLabel31["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel31["BackgroundTransparency"] = 1
	ImageLabel31["Rotation"] = 55
	ImageLabel31["Image"] = "rbxassetid://9875928470"
	ImageLabel31["Name"] = "PufferFish"
	ImageLabel31["Position"] = UDim2.new(-0.0421274602, 0, 0.836252451, 0)
	ImageLabel31["Size"] = UDim2.new(0, 70, 0, 58)
	ImageLabel31["ZIndex"] = 7
	ImageLabel31["BorderSizePixel"] = 0
	ImageLabel31["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel31----
	--Script32--
	local Script32 = Instance.new("Script",ImageLabel31)
	----Script32----
	--Frame33--
	local Frame33 = Instance.new("Frame",Frame2)
	Frame33["Size"] = UDim2.new(0, 185, 0, 350)
	Frame33["Position"] = UDim2.new(-0.181326076, 0, 0, 0)
	Frame33["BorderColor3"] = Color3.new(0, 0, 0)
	Frame33["ZIndex"] = 2
	Frame33["BorderSizePixel"] = 0
	Frame33["BackgroundColor3"] = Color3.new(0.0196078, 0.415686, 0.317647)
	----Frame33----
	--UIStroke34--
	local UIStroke34 = Instance.new("UIStroke",Frame33)
	UIStroke34["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke34["Thickness"] = 5
	----UIStroke34----
	--ScrollingFrame35--
	local ScrollingFrame35 = Instance.new("ScrollingFrame",Frame33)
	ScrollingFrame35["ScrollBarImageColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame35["MidImage"] = ""
	ScrollingFrame35["Active"] = true
	ScrollingFrame35["BorderColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame35["AutomaticCanvasSize"] = Enum.AutomaticSize.XY
	ScrollingFrame35["ScrollBarImageTransparency"] = 1
	ScrollingFrame35["TopImage"] = ""
	ScrollingFrame35["Position"] = UDim2.new(-0.00540540554, 0, 0.176866278, 0)
	ScrollingFrame35["Size"] = UDim2.new(0, 185, 0, 288)
	ScrollingFrame35["BottomImage"] = ""
	ScrollingFrame35["BorderSizePixel"] = 0
	ScrollingFrame35["BackgroundColor3"] = Color3.new(0.0156863, 0.321569, 0.32549)
	----ScrollingFrame35----
	--TextButton36--
	local TextButton36 = Instance.new("TextButton",ScrollingFrame35)
	TextButton36["TextColor3"] = Color3.new(0, 0, 0)
	TextButton36["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton36["Text"] = ""
	TextButton36["TextStrokeTransparency"] = 0
	TextButton36["TextSize"] = 14
	TextButton36["Font"] = Enum.Font.SourceSans
	TextButton36["BackgroundTransparency"] = 1
	TextButton36["Position"] = UDim2.new(0.0819209069, 0, 0, 0)
	TextButton36["Name"] = "NONO"
	TextButton36["Size"] = UDim2.new(0, 148, 0, 6)
	TextButton36["BorderSizePixel"] = 0
	TextButton36["BackgroundColor3"] = Color3.new(1, 1, 1)
	----TextButton36----
	--UIListLayout37--
	local UIListLayout37 = Instance.new("UIListLayout",ScrollingFrame35)
	UIListLayout37["Padding"] = UDim.new(0, 16)
	UIListLayout37["HorizontalAlignment"] = Enum.HorizontalAlignment.Center
	UIListLayout37["SortOrder"] = Enum.SortOrder.LayoutOrder
	----UIListLayout37----
	--UIStroke38--
	local UIStroke38 = Instance.new("UIStroke",ScrollingFrame35)
	UIStroke38["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke38["Thickness"] = 5
	----UIStroke38----
	--TextButton39--
	local TextButton39 = Instance.new("TextButton",ScrollingFrame35)
	TextButton39["TextWrapped"] = true
	TextButton39["TextColor3"] = Color3.new(1, 1, 1)
	TextButton39["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton39["Text"] = "Home"
	TextButton39["TextStrokeTransparency"] = 0
	TextButton39["TextSize"] = 14
	TextButton39["Font"] = Enum.Font.FredokaOne
	TextButton39["Name"] = "HomeButton"
	TextButton39["Position"] = UDim2.new(0.211864412, 0, 0.825083494, 0)
	TextButton39["TextScaled"] = true
	TextButton39["Size"] = UDim2.new(0, 122, 0, 42)
	TextButton39["BorderSizePixel"] = 0
	TextButton39["BackgroundColor3"] = Color3.new(0.0352941, 0.741176, 0.741176)
	----TextButton39----
	--UIStroke40--
	local UIStroke40 = Instance.new("UIStroke",TextButton39)
	UIStroke40["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke40["Thickness"] = 5
	----UIStroke40----
	--UICorner41--
	local UICorner41 = Instance.new("UICorner",TextButton39)
	UICorner41["CornerRadius"] = UDim.new(0, 4)
	----UICorner41----
	--Script42--
	local Script42 = Instance.new("Script",TextButton39)
	Script42["Name"] = "ButtonHover"
	----Script42----
	--Script43--
	local Script43 = Instance.new("Script",TextButton39)
	Script43["Name"] = "ButtonHover"
	----Script43----
	--TextLabel44--
	local TextLabel44 = Instance.new("TextLabel",ScrollingFrame35)
	TextLabel44["TextWrapped"] = true
	TextLabel44["TextColor3"] = Color3.new(0.137255, 0.984314, 1)
	TextLabel44["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel44["Text"] = "=[scripts below]="
	TextLabel44["TextStrokeTransparency"] = 0
	TextLabel44["TextSize"] = 14
	TextLabel44["Font"] = Enum.Font.Unknown
	TextLabel44["BackgroundTransparency"] = 1
	TextLabel44["Position"] = UDim2.new(0, 0, 0.0514285713, 0)
	TextLabel44["TextScaled"] = true
	TextLabel44["Size"] = UDim2.new(0, 177, 0, 43)
	TextLabel44["BorderSizePixel"] = 0
	TextLabel44["BackgroundColor3"] = Color3.new(1, 1, 1)
	----TextLabel44----
	--UICorner45--
	local UICorner45 = Instance.new("UICorner",TextLabel44)
	UICorner45["CornerRadius"] = UDim.new(0, 4)
	----UICorner45----
	--Script46--
	local Script46 = Instance.new("Script",TextLabel44)
	Script46["Name"] = "ButtonHover"
	----Script46----
	--TextButton47--
	local TextButton47 = Instance.new("TextButton",ScrollingFrame35)
	TextButton47["TextWrapped"] = true
	TextButton47["TextColor3"] = Color3.new(1, 1, 1)
	TextButton47["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton47["Text"] = "Fisch"
	TextButton47["TextStrokeTransparency"] = 0
	TextButton47["TextSize"] = 14
	TextButton47["Font"] = Enum.Font.FredokaOne
	TextButton47["Name"] = "FischButton"
	TextButton47["Position"] = UDim2.new(0.211864412, 0, 0.825083494, 0)
	TextButton47["TextScaled"] = true
	TextButton47["Size"] = UDim2.new(0, 122, 0, 42)
	TextButton47["BorderSizePixel"] = 0
	TextButton47["BackgroundColor3"] = Color3.new(0.0352941, 0.741176, 0.741176)
	----TextButton47----
	--UIStroke48--
	local UIStroke48 = Instance.new("UIStroke",TextButton47)
	UIStroke48["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke48["Thickness"] = 5
	----UIStroke48----
	--UICorner49--
	local UICorner49 = Instance.new("UICorner",TextButton47)
	UICorner49["CornerRadius"] = UDim.new(0, 4)
	----UICorner49----
	--Script50--
	local Script50 = Instance.new("Script",TextButton47)
	Script50["Name"] = "ButtonHover"
	----Script50----
	--Script51--
	local Script51 = Instance.new("Script",TextButton47)
	Script51["Name"] = "ButtonHover"
	----Script51----
	--TextButton52--
	local TextButton52 = Instance.new("TextButton",ScrollingFrame35)
	TextButton52["TextWrapped"] = true
	TextButton52["TextColor3"] = Color3.new(1, 1, 1)
	TextButton52["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton52["Text"] = "Realistic"
	TextButton52["TextStrokeTransparency"] = 0
	TextButton52["TextSize"] = 14
	TextButton52["Font"] = Enum.Font.FredokaOne
	TextButton52["Name"] = "RealisticButton"
	TextButton52["Position"] = UDim2.new(0.211864412, 0, 0.825083494, 0)
	TextButton52["TextScaled"] = true
	TextButton52["Size"] = UDim2.new(0, 122, 0, 42)
	TextButton52["BorderSizePixel"] = 0
	TextButton52["BackgroundColor3"] = Color3.new(0.0352941, 0.741176, 0.741176)
	----TextButton52----
	--UIStroke53--
	local UIStroke53 = Instance.new("UIStroke",TextButton52)
	UIStroke53["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke53["Thickness"] = 5
	----UIStroke53----
	--UICorner54--
	local UICorner54 = Instance.new("UICorner",TextButton52)
	UICorner54["CornerRadius"] = UDim.new(0, 4)
	----UICorner54----
	--Script55--
	local Script55 = Instance.new("Script",TextButton52)
	Script55["Name"] = "ButtonHover"
	----Script55----
	--Script56--
	local Script56 = Instance.new("Script",TextButton52)
	Script56["Name"] = "ButtonHover"
	----Script56----
	--TextButton57--
	local TextButton57 = Instance.new("TextButton",ScrollingFrame35)
	TextButton57["TextWrapped"] = true
	TextButton57["TextColor3"] = Color3.new(1, 1, 1)
	TextButton57["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton57["Text"] = "Arsenal"
	TextButton57["TextStrokeTransparency"] = 0
	TextButton57["TextSize"] = 14
	TextButton57["Font"] = Enum.Font.FredokaOne
	TextButton57["Name"] = "ArsenalButton"
	TextButton57["Position"] = UDim2.new(0.211864412, 0, 0.825083494, 0)
	TextButton57["TextScaled"] = true
	TextButton57["Size"] = UDim2.new(0, 122, 0, 42)
	TextButton57["BorderSizePixel"] = 0
	TextButton57["BackgroundColor3"] = Color3.new(0.0352941, 0.741176, 0.741176)
	----TextButton57----
	--UIStroke58--
	local UIStroke58 = Instance.new("UIStroke",TextButton57)
	UIStroke58["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke58["Thickness"] = 5
	----UIStroke58----
	--UICorner59--
	local UICorner59 = Instance.new("UICorner",TextButton57)
	UICorner59["CornerRadius"] = UDim.new(0, 4)
	----UICorner59----
	--Script60--
	local Script60 = Instance.new("Script",TextButton57)
	Script60["Name"] = "ButtonHover"
	----Script60----
	--Script61--
	local Script61 = Instance.new("Script",TextButton57)
	Script61["Name"] = "ButtonHover"
	----Script61----
	--TextButton62--
	local TextButton62 = Instance.new("TextButton",ScrollingFrame35)
	TextButton62["TextWrapped"] = true
	TextButton62["TextColor3"] = Color3.new(1, 1, 1)
	TextButton62["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton62["Text"] = "Rivals"
	TextButton62["TextStrokeTransparency"] = 0
	TextButton62["TextSize"] = 14
	TextButton62["Font"] = Enum.Font.FredokaOne
	TextButton62["Name"] = "RivalsButton"
	TextButton62["Position"] = UDim2.new(0.211864412, 0, 0.825083494, 0)
	TextButton62["TextScaled"] = true
	TextButton62["Size"] = UDim2.new(0, 122, 0, 42)
	TextButton62["BorderSizePixel"] = 0
	TextButton62["BackgroundColor3"] = Color3.new(0.0352941, 0.741176, 0.741176)
	----TextButton62----
	--UIStroke63--
	local UIStroke63 = Instance.new("UIStroke",TextButton62)
	UIStroke63["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke63["Thickness"] = 5
	----UIStroke63----
	--UICorner64--
	local UICorner64 = Instance.new("UICorner",TextButton62)
	UICorner64["CornerRadius"] = UDim.new(0, 4)
	----UICorner64----
	--Script65--
	local Script65 = Instance.new("Script",TextButton62)
	Script65["Name"] = "ButtonHover"
	----Script65----
	--Script66--
	local Script66 = Instance.new("Script",TextButton62)
	Script66["Name"] = "ButtonHover"
	----Script66----
	--TextLabel67--
	local TextLabel67 = Instance.new("TextLabel",ScrollingFrame35)
	TextLabel67["TextWrapped"] = true
	TextLabel67["TextColor3"] = Color3.new(0.137255, 0.984314, 1)
	TextLabel67["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel67["Text"] = "MORE SOON"
	TextLabel67["TextStrokeTransparency"] = 0
	TextLabel67["TextSize"] = 14
	TextLabel67["Font"] = Enum.Font.Unknown
	TextLabel67["BackgroundTransparency"] = 1
	TextLabel67["Position"] = UDim2.new(0.0216216221, 0, 0.336805552, 0)
	TextLabel67["TextScaled"] = true
	TextLabel67["Size"] = UDim2.new(0, 174, 0, 32)
	TextLabel67["BorderSizePixel"] = 0
	TextLabel67["BackgroundColor3"] = Color3.new(1, 1, 1)
	----TextLabel67----
	--UICorner68--
	local UICorner68 = Instance.new("UICorner",TextLabel67)
	UICorner68["CornerRadius"] = UDim.new(0, 4)
	----UICorner68----
	--Script69--
	local Script69 = Instance.new("Script",TextLabel67)
	Script69["Name"] = "ButtonHover"
	----Script69----
	--UICorner70--
	local UICorner70 = Instance.new("UICorner",Frame33)
	UICorner70["CornerRadius"] = UDim.new(0, 4)
	----UICorner70----
	--TextLabel71--
	local TextLabel71 = Instance.new("TextLabel",Frame33)
	TextLabel71["TextWrapped"] = true
	TextLabel71["TextColor3"] = Color3.new(0.137255, 0.984314, 1)
	TextLabel71["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel71["Text"] = "garry, larry, and barry"
	TextLabel71["TextStrokeTransparency"] = 0
	TextLabel71["Font"] = Enum.Font.Unknown
	TextLabel71["BackgroundTransparency"] = 1
	TextLabel71["TextSize"] = 14
	TextLabel71["TextScaled"] = true
	TextLabel71["Size"] = UDim2.new(0, 177, 0, 34)
	TextLabel71["BorderSizePixel"] = 0
	TextLabel71["BackgroundColor3"] = Color3.new(1, 1, 1)
	----TextLabel71----
	--UICorner72--
	local UICorner72 = Instance.new("UICorner",TextLabel71)
	UICorner72["CornerRadius"] = UDim.new(0, 4)
	----UICorner72----
	--Script73--
	local Script73 = Instance.new("Script",TextLabel71)
	Script73["Name"] = "ButtonHover"
	----Script73----
	--TextLabel74--
	local TextLabel74 = Instance.new("TextLabel",Frame33)
	TextLabel74["TextWrapped"] = true
	TextLabel74["TextColor3"] = Color3.new(0.137255, 0.984314, 1)
	TextLabel74["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel74["Text"] = "hub"
	TextLabel74["TextStrokeTransparency"] = 0
	TextLabel74["TextSize"] = 14
	TextLabel74["Font"] = Enum.Font.Unknown
	TextLabel74["BackgroundTransparency"] = 1
	TextLabel74["Position"] = UDim2.new(0, 0, 0.0514285713, 0)
	TextLabel74["TextScaled"] = true
	TextLabel74["Size"] = UDim2.new(0, 177, 0, 43)
	TextLabel74["BorderSizePixel"] = 0
	TextLabel74["BackgroundColor3"] = Color3.new(1, 1, 1)
	----TextLabel74----
	--UICorner75--
	local UICorner75 = Instance.new("UICorner",TextLabel74)
	UICorner75["CornerRadius"] = UDim.new(0, 4)
	----UICorner75----
	--Script76--
	local Script76 = Instance.new("Script",TextLabel74)
	Script76["Name"] = "ButtonHover"
	----Script76----
	--Frame77--
	local Frame77 = Instance.new("Frame",Frame2)
	Frame77["Size"] = UDim2.new(0, 630, 0, 349)
	Frame77["BackgroundTransparency"] = 1
	Frame77["Position"] = UDim2.new(0.0159126688, 0, -0.000813337043, 0)
	Frame77["BorderColor3"] = Color3.new(0, 0, 0)
	Frame77["Name"] = "Home"
	Frame77["BorderSizePixel"] = 0
	Frame77["BackgroundColor3"] = Color3.new(0.0313726, 0.635294, 0.482353)
	----Frame77----
	--UICorner78--
	local UICorner78 = Instance.new("UICorner",Frame77)
	UICorner78["CornerRadius"] = UDim.new(0, 4)
	----UICorner78----
	--UIStroke79--
	local UIStroke79 = Instance.new("UIStroke",Frame77)
	UIStroke79["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke79["Thickness"] = 5
	----UIStroke79----
	--TextLabel80--
	local TextLabel80 = Instance.new("TextLabel",Frame77)
	TextLabel80["TextWrapped"] = true
	TextLabel80["TextColor3"] = Color3.new(0.137255, 0.984314, 1)
	TextLabel80["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel80["Text"] = "thanks for using garry's hub, this was all made by 3 autistic people"
	TextLabel80["TextStrokeTransparency"] = 0
	TextLabel80["TextSize"] = 14
	TextLabel80["Font"] = Enum.Font.Unknown
	TextLabel80["BackgroundTransparency"] = 1
	TextLabel80["Position"] = UDim2.new(0.100771271, 0, 0.0257142521, 0)
	TextLabel80["TextScaled"] = true
	TextLabel80["Size"] = UDim2.new(0, 564, 0, 43)
	TextLabel80["BorderSizePixel"] = 0
	TextLabel80["BackgroundColor3"] = Color3.new(1, 1, 1)
	----TextLabel80----
	--UICorner81--
	local UICorner81 = Instance.new("UICorner",TextLabel80)
	UICorner81["CornerRadius"] = UDim.new(0, 4)
	----UICorner81----
	--Script82--
	local Script82 = Instance.new("Script",TextLabel80)
	Script82["Name"] = "ButtonHover"
	----Script82----
	--TextLabel83--
	local TextLabel83 = Instance.new("TextLabel",Frame77)
	TextLabel83["TextWrapped"] = true
	TextLabel83["TextColor3"] = Color3.new(0.137255, 0.984314, 1)
	TextLabel83["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel83["Text"] = " patch notes"
	TextLabel83["TextStrokeTransparency"] = 0
	TextLabel83["Font"] = Enum.Font.FredokaOne
	TextLabel83["TextSize"] = 14
	TextLabel83["Position"] = UDim2.new(0.202194363, 0, 0.257332593, 0)
	TextLabel83["TextScaled"] = true
	TextLabel83["Size"] = UDim2.new(0, 427, 0, 74)
	TextLabel83["BorderSizePixel"] = 0
	TextLabel83["BackgroundColor3"] = Color3.new(0.0156863, 0.321569, 0.32549)
	----TextLabel83----
	--Script84--
	local Script84 = Instance.new("Script",TextLabel83)
	Script84["Name"] = "ButtonHover"
	----Script84----
	--UICorner85--
	local UICorner85 = Instance.new("UICorner",TextLabel83)
	UICorner85["CornerRadius"] = UDim.new(0, 4)
	----UICorner85----
	--UIStroke86--
	local UIStroke86 = Instance.new("UIStroke",TextLabel83)
	UIStroke86["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke86["Thickness"] = 5
	----UIStroke86----
	--TextLabel87--
	local TextLabel87 = Instance.new("TextLabel",Frame77)
	TextLabel87["TextWrapped"] = true
	TextLabel87["TextColor3"] = Color3.new(0, 1, 0.14902)
	TextLabel87["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel87["Text"] = "literally nothing yet, we barely just deployed v1.1"
	TextLabel87["TextStrokeTransparency"] = 0
	TextLabel87["TextSize"] = 14
	TextLabel87["Font"] = Enum.Font.FredokaOne
	TextLabel87["BackgroundTransparency"] = 1
	TextLabel87["Position"] = UDim2.new(0.153605014, 0, 0.548761189, 0)
	TextLabel87["TextScaled"] = true
	TextLabel87["Size"] = UDim2.new(0, 489, 0, 27)
	TextLabel87["BorderSizePixel"] = 0
	TextLabel87["BackgroundColor3"] = Color3.new(0.0156863, 0.321569, 0.32549)
	----TextLabel87----
	--Script88--
	local Script88 = Instance.new("Script",TextLabel87)
	Script88["Name"] = "ButtonHover"
	----Script88----
	--TextLabel89--
	local TextLabel89 = Instance.new("TextLabel",Frame77)
	TextLabel89["TextWrapped"] = true
	TextLabel89["TextColor3"] = Color3.new(1, 0, 0)
	TextLabel89["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel89["Text"] = "- removed old ui hub, only testing ones are up"
	TextLabel89["TextStrokeTransparency"] = 0
	TextLabel89["TextSize"] = 14
	TextLabel89["Font"] = Enum.Font.FredokaOne
	TextLabel89["BackgroundTransparency"] = 1
	TextLabel89["Position"] = UDim2.new(0.153605044, 0, 0.692027628, 0)
	TextLabel89["TextScaled"] = true
	TextLabel89["Size"] = UDim2.new(0, 489, 0, 27)
	TextLabel89["BorderSizePixel"] = 0
	TextLabel89["BackgroundColor3"] = Color3.new(0.0156863, 0.321569, 0.32549)
	----TextLabel89----
	--Script90--
	local Script90 = Instance.new("Script",TextLabel89)
	Script90["Name"] = "ButtonHover"
	----Script90----
	--TextLabel91--
	local TextLabel91 = Instance.new("TextLabel",Frame77)
	TextLabel91["TextWrapped"] = true
	TextLabel91["TextColor3"] = Color3.new(0, 1, 0.14902)
	TextLabel91["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel91["Text"] = "+ added vfx (the pufferfish, squid, and bubbles)"
	TextLabel91["TextStrokeTransparency"] = 0
	TextLabel91["TextSize"] = 14
	TextLabel91["Font"] = Enum.Font.FredokaOne
	TextLabel91["BackgroundTransparency"] = 1
	TextLabel91["Position"] = UDim2.new(0.153605044, 0, 0.617529035, 0)
	TextLabel91["TextScaled"] = true
	TextLabel91["Size"] = UDim2.new(0, 489, 0, 27)
	TextLabel91["BorderSizePixel"] = 0
	TextLabel91["BackgroundColor3"] = Color3.new(0.0156863, 0.321569, 0.32549)
	----TextLabel91----
	--Script92--
	local Script92 = Instance.new("Script",TextLabel91)
	Script92["Name"] = "ButtonHover"
	----Script92----
	--TextLabel93--
	local TextLabel93 = Instance.new("TextLabel",Frame77)
	TextLabel93["TextWrapped"] = true
	TextLabel93["TextColor3"] = Color3.new(0, 0.94902, 1)
	TextLabel93["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel93["Text"] = "nothing yet, please wait for an update!"
	TextLabel93["TextStrokeTransparency"] = 0
	TextLabel93["TextSize"] = 14
	TextLabel93["Font"] = Enum.Font.FredokaOne
	TextLabel93["BackgroundTransparency"] = 1
	TextLabel93["Position"] = UDim2.new(0.153605044, 0, 0.812371969, 0)
	TextLabel93["TextScaled"] = true
	TextLabel93["Size"] = UDim2.new(0, 489, 0, 48)
	TextLabel93["BorderSizePixel"] = 0
	TextLabel93["BackgroundColor3"] = Color3.new(0.0156863, 0.321569, 0.32549)
	----TextLabel93----
	--Script94--
	local Script94 = Instance.new("Script",TextLabel93)
	Script94["Name"] = "ButtonHover"
	----Script94----
	--Frame95--
	local Frame95 = Instance.new("Frame",Frame2)
	Frame95["Visible"] = false
	Frame95["ClipsDescendants"] = true
	Frame95["Name"] = "Fisch"
	Frame95["Position"] = UDim2.new(0.0159126688, 0, -0.000813337043, 0)
	Frame95["BorderColor3"] = Color3.new(0, 0, 0)
	Frame95["Size"] = UDim2.new(0, 630, 0, 349)
	Frame95["BorderSizePixel"] = 0
	Frame95["BackgroundColor3"] = Color3.new(0.0313726, 0.635294, 0.482353)
	----Frame95----
	--UICorner96--
	local UICorner96 = Instance.new("UICorner",Frame95)
	UICorner96["CornerRadius"] = UDim.new(0, 4)
	----UICorner96----
	--UIStroke97--
	local UIStroke97 = Instance.new("UIStroke",Frame95)
	UIStroke97["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke97["Thickness"] = 5
	----UIStroke97----
	--ScrollingFrame98--
	local ScrollingFrame98 = Instance.new("ScrollingFrame",Frame95)
	ScrollingFrame98["ScrollBarImageColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame98["Size"] = UDim2.new(0, 568, 0, 350)
	ScrollingFrame98["BackgroundTransparency"] = 1
	ScrollingFrame98["Active"] = true
	ScrollingFrame98["BorderColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame98["Position"] = UDim2.new(0.100000001, 0, 0, 0)
	ScrollingFrame98["BorderSizePixel"] = 0
	ScrollingFrame98["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ScrollingFrame98----
	--UICorner99--
	local UICorner99 = Instance.new("UICorner",ScrollingFrame98)
	UICorner99["CornerRadius"] = UDim.new(0, 4)
	----UICorner99----
	--UIListLayout100--
	local UIListLayout100 = Instance.new("UIListLayout",ScrollingFrame98)
	UIListLayout100["Padding"] = UDim.new(0, 16)
	UIListLayout100["HorizontalAlignment"] = Enum.HorizontalAlignment.Center
	UIListLayout100["SortOrder"] = Enum.SortOrder.LayoutOrder
	----UIListLayout100----
	--TextButton101--
	local TextButton101 = Instance.new("TextButton",ScrollingFrame98)
	TextButton101["TextWrapped"] = true
	TextButton101["TextColor3"] = Color3.new(1, 1, 1)
	TextButton101["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton101["Text"] = "NAOKI"
	TextButton101["TextStrokeTransparency"] = 0
	TextButton101["TextSize"] = 14
	TextButton101["Font"] = Enum.Font.FredokaOne
	TextButton101["Name"] = "naokihub"
	TextButton101["Position"] = UDim2.new(0.0771929845, 0, 1.26887119, 0)
	TextButton101["TextScaled"] = true
	TextButton101["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton101["BorderSizePixel"] = 0
	TextButton101["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton101----
	--Script102--
	local Script102 = Instance.new("Script",TextButton101)
	Script102["Name"] = "Execute"
	----Script102----
	--Script103--
	local Script103 = Instance.new("Script",TextButton101)
	----Script103----
	--UICorner104--
	local UICorner104 = Instance.new("UICorner",TextButton101)
	UICorner104["CornerRadius"] = UDim.new(0, 4)
	----UICorner104----
	--UIStroke105--
	local UIStroke105 = Instance.new("UIStroke",TextButton101)
	UIStroke105["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke105["Thickness"] = 3
	UIStroke105["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke105----
	--Script106--
	local Script106 = Instance.new("Script",TextButton101)
	Script106["Name"] = "ButtonHover"
	----Script106----
	--TextButton107--
	local TextButton107 = Instance.new("TextButton",ScrollingFrame98)
	TextButton107["TextWrapped"] = true
	TextButton107["TextColor3"] = Color3.new(1, 1, 1)
	TextButton107["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton107["Text"] = "KICIAHOOK"
	TextButton107["TextStrokeTransparency"] = 0
	TextButton107["TextSize"] = 14
	TextButton107["Font"] = Enum.Font.FredokaOne
	TextButton107["Name"] = "kiciahook"
	TextButton107["Position"] = UDim2.new(0.0771929845, 0, 1.11026227, 0)
	TextButton107["TextScaled"] = true
	TextButton107["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton107["BorderSizePixel"] = 0
	TextButton107["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton107----
	--Script108--
	local Script108 = Instance.new("Script",TextButton107)
	Script108["Name"] = "Execute"
	----Script108----
	--Script109--
	local Script109 = Instance.new("Script",TextButton107)
	----Script109----
	--UIStroke110--
	local UIStroke110 = Instance.new("UIStroke",TextButton107)
	UIStroke110["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke110["Thickness"] = 3
	UIStroke110["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke110----
	--UICorner111--
	local UICorner111 = Instance.new("UICorner",TextButton107)
	UICorner111["CornerRadius"] = UDim.new(0, 4)
	----UICorner111----
	--Script112--
	local Script112 = Instance.new("Script",TextButton107)
	Script112["Name"] = "ButtonHover"
	----Script112----
	--TextButton113--
	local TextButton113 = Instance.new("TextButton",ScrollingFrame98)
	TextButton113["RichText"] = true
	TextButton113["TextColor3"] = Color3.new(1, 1, 1)
	TextButton113["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton113["Text"] = "SpeedHub X"
	TextButton113["TextWrapped"] = true
	TextButton113["TextStrokeTransparency"] = 0
	TextButton113["TextSize"] = 14
	TextButton113["Font"] = Enum.Font.FredokaOne
	TextButton113["Name"] = "SpeedHub X"
	TextButton113["Position"] = UDim2.new(0.0771929845, 0, 0, 0)
	TextButton113["TextScaled"] = true
	TextButton113["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton113["BorderSizePixel"] = 0
	TextButton113["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton113----
	--UICorner114--
	local UICorner114 = Instance.new("UICorner",TextButton113)
	UICorner114["CornerRadius"] = UDim.new(0, 4)
	----UICorner114----
	--UIStroke115--
	local UIStroke115 = Instance.new("UIStroke",TextButton113)
	UIStroke115["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke115["Thickness"] = 3
	UIStroke115["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke115----
	--Script116--
	local Script116 = Instance.new("Script",TextButton113)
	----Script116----
	--Script117--
	local Script117 = Instance.new("Script",TextButton113)
	Script117["Name"] = "Execute"
	----Script117----
	--Script118--
	local Script118 = Instance.new("Script",TextButton113)
	Script118["Name"] = "ButtonHover"
	----Script118----
	--TextButton119--
	local TextButton119 = Instance.new("TextButton",ScrollingFrame98)
	TextButton119["TextWrapped"] = true
	TextButton119["TextColor3"] = Color3.new(1, 1, 1)
	TextButton119["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton119["Text"] = "RAITO"
	TextButton119["TextStrokeTransparency"] = 0
	TextButton119["TextSize"] = 14
	TextButton119["Font"] = Enum.Font.FredokaOne
	TextButton119["Name"] = "RAITO"
	TextButton119["Position"] = UDim2.new(0.0771929845, 0, 0.951653421, 0)
	TextButton119["TextScaled"] = true
	TextButton119["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton119["BorderSizePixel"] = 0
	TextButton119["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton119----
	--Script120--
	local Script120 = Instance.new("Script",TextButton119)
	Script120["Name"] = "Execute"
	----Script120----
	--UICorner121--
	local UICorner121 = Instance.new("UICorner",TextButton119)
	UICorner121["CornerRadius"] = UDim.new(0, 4)
	----UICorner121----
	--UIStroke122--
	local UIStroke122 = Instance.new("UIStroke",TextButton119)
	UIStroke122["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke122["Thickness"] = 3
	UIStroke122["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke122----
	--Script123--
	local Script123 = Instance.new("Script",TextButton119)
	----Script123----
	--Script124--
	local Script124 = Instance.new("Script",TextButton119)
	Script124["Name"] = "ButtonHover"
	----Script124----
	--TextButton125--
	local TextButton125 = Instance.new("TextButton",ScrollingFrame98)
	TextButton125["TextWrapped"] = true
	TextButton125["TextColor3"] = Color3.new(1, 1, 1)
	TextButton125["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton125["Text"] = "NYX"
	TextButton125["TextStrokeTransparency"] = 0
	TextButton125["TextSize"] = 14
	TextButton125["Font"] = Enum.Font.FredokaOne
	TextButton125["Name"] = "NYX"
	TextButton125["Position"] = UDim2.new(0.0771929845, 0, 0.793044508, 0)
	TextButton125["TextScaled"] = true
	TextButton125["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton125["BorderSizePixel"] = 0
	TextButton125["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton125----
	--Script126--
	local Script126 = Instance.new("Script",TextButton125)
	Script126["Name"] = "Execute"
	----Script126----
	--UICorner127--
	local UICorner127 = Instance.new("UICorner",TextButton125)
	UICorner127["CornerRadius"] = UDim.new(0, 4)
	----UICorner127----
	--UIStroke128--
	local UIStroke128 = Instance.new("UIStroke",TextButton125)
	UIStroke128["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke128["Thickness"] = 3
	UIStroke128["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke128----
	--Script129--
	local Script129 = Instance.new("Script",TextButton125)
	----Script129----
	--Script130--
	local Script130 = Instance.new("Script",TextButton125)
	Script130["Name"] = "ButtonHover"
	----Script130----
	--TextButton131--
	local TextButton131 = Instance.new("TextButton",ScrollingFrame98)
	TextButton131["TextWrapped"] = true
	TextButton131["TextColor3"] = Color3.new(1, 1, 1)
	TextButton131["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton131["Text"] = "NATIVE"
	TextButton131["TextStrokeTransparency"] = 0
	TextButton131["TextSize"] = 14
	TextButton131["Font"] = Enum.Font.FredokaOne
	TextButton131["Name"] = "NATIVE"
	TextButton131["Position"] = UDim2.new(0.0771929845, 0, 0.634435594, 0)
	TextButton131["TextScaled"] = true
	TextButton131["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton131["BorderSizePixel"] = 0
	TextButton131["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton131----
	--Script132--
	local Script132 = Instance.new("Script",TextButton131)
	Script132["Name"] = "Execute"
	----Script132----
	--UICorner133--
	local UICorner133 = Instance.new("UICorner",TextButton131)
	UICorner133["CornerRadius"] = UDim.new(0, 4)
	----UICorner133----
	--UIStroke134--
	local UIStroke134 = Instance.new("UIStroke",TextButton131)
	UIStroke134["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke134["Thickness"] = 3
	UIStroke134["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke134----
	--Script135--
	local Script135 = Instance.new("Script",TextButton131)
	----Script135----
	--Script136--
	local Script136 = Instance.new("Script",TextButton131)
	Script136["Name"] = "ButtonHover"
	----Script136----
	--TextButton137--
	local TextButton137 = Instance.new("TextButton",ScrollingFrame98)
	TextButton137["TextWrapped"] = true
	TextButton137["TextColor3"] = Color3.new(1, 1, 1)
	TextButton137["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton137["Text"] = "LUNOR"
	TextButton137["TextStrokeTransparency"] = 0
	TextButton137["TextSize"] = 14
	TextButton137["Font"] = Enum.Font.FredokaOne
	TextButton137["Name"] = "LUNOR"
	TextButton137["Position"] = UDim2.new(0.0771929845, 0, 0.47582671, 0)
	TextButton137["TextScaled"] = true
	TextButton137["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton137["BorderSizePixel"] = 0
	TextButton137["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton137----
	--Script138--
	local Script138 = Instance.new("Script",TextButton137)
	Script138["Name"] = "Execute"
	----Script138----
	--UICorner139--
	local UICorner139 = Instance.new("UICorner",TextButton137)
	UICorner139["CornerRadius"] = UDim.new(0, 4)
	----UICorner139----
	--UIStroke140--
	local UIStroke140 = Instance.new("UIStroke",TextButton137)
	UIStroke140["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke140["Thickness"] = 3
	UIStroke140["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke140----
	--Script141--
	local Script141 = Instance.new("Script",TextButton137)
	----Script141----
	--Script142--
	local Script142 = Instance.new("Script",TextButton137)
	Script142["Name"] = "ButtonHover"
	----Script142----
	--TextButton143--
	local TextButton143 = Instance.new("TextButton",ScrollingFrame98)
	TextButton143["RichText"] = true
	TextButton143["TextColor3"] = Color3.new(1, 1, 1)
	TextButton143["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton143["Text"] = "KICIAHOOK"
	TextButton143["TextWrapped"] = true
	TextButton143["TextStrokeTransparency"] = 0
	TextButton143["TextSize"] = 14
	TextButton143["Font"] = Enum.Font.FredokaOne
	TextButton143["Name"] = "Kiciahook"
	TextButton143["Position"] = UDim2.new(0.0771929845, 0, 0.158608899, 0)
	TextButton143["TextScaled"] = true
	TextButton143["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton143["BorderSizePixel"] = 0
	TextButton143["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton143----
	--Script144--
	local Script144 = Instance.new("Script",TextButton143)
	Script144["Name"] = "Execute"
	----Script144----
	--UIStroke145--
	local UIStroke145 = Instance.new("UIStroke",TextButton143)
	UIStroke145["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke145["Thickness"] = 3
	UIStroke145["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke145----
	--UICorner146--
	local UICorner146 = Instance.new("UICorner",TextButton143)
	UICorner146["CornerRadius"] = UDim.new(0, 4)
	----UICorner146----
	--Script147--
	local Script147 = Instance.new("Script",TextButton143)
	----Script147----
	--Script148--
	local Script148 = Instance.new("Script",TextButton143)
	Script148["Name"] = "ButtonHover"
	----Script148----
	--TextButton149--
	local TextButton149 = Instance.new("TextButton",ScrollingFrame98)
	TextButton149["TextWrapped"] = true
	TextButton149["TextColor3"] = Color3.new(1, 1, 1)
	TextButton149["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton149["Text"] = "KNCRYPT"
	TextButton149["TextStrokeTransparency"] = 0
	TextButton149["TextSize"] = 14
	TextButton149["Font"] = Enum.Font.FredokaOne
	TextButton149["Name"] = "KNCRYPT"
	TextButton149["Position"] = UDim2.new(0.0771929845, 0, 0.317217797, 0)
	TextButton149["TextScaled"] = true
	TextButton149["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton149["BorderSizePixel"] = 0
	TextButton149["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton149----
	--Script150--
	local Script150 = Instance.new("Script",TextButton149)
	Script150["Name"] = "Execute"
	----Script150----
	--UICorner151--
	local UICorner151 = Instance.new("UICorner",TextButton149)
	UICorner151["CornerRadius"] = UDim.new(0, 4)
	----UICorner151----
	--UIStroke152--
	local UIStroke152 = Instance.new("UIStroke",TextButton149)
	UIStroke152["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke152["Thickness"] = 3
	UIStroke152["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke152----
	--Script153--
	local Script153 = Instance.new("Script",TextButton149)
	----Script153----
	--Script154--
	local Script154 = Instance.new("Script",TextButton149)
	Script154["Name"] = "ButtonHover"
	----Script154----
	--TextButton155--
	local TextButton155 = Instance.new("TextButton",ScrollingFrame98)
	TextButton155["TextWrapped"] = true
	TextButton155["TextColor3"] = Color3.new(1, 1, 1)
	TextButton155["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton155["Text"] = "AVERAGE"
	TextButton155["TextStrokeTransparency"] = 0
	TextButton155["TextSize"] = 14
	TextButton155["Font"] = Enum.Font.FredokaOne
	TextButton155["Name"] = "AVERAGE"
	TextButton155["Position"] = UDim2.new(0.0686619729, 0, 0.74000001, 0)
	TextButton155["TextScaled"] = true
	TextButton155["Size"] = UDim2.new(0, 510, 0, 49)
	TextButton155["BorderSizePixel"] = 0
	TextButton155["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton155----
	--Script156--
	local Script156 = Instance.new("Script",TextButton155)
	Script156["Name"] = "Execute"
	----Script156----
	--UICorner157--
	local UICorner157 = Instance.new("UICorner",TextButton155)
	UICorner157["CornerRadius"] = UDim.new(0, 4)
	----UICorner157----
	--UIStroke158--
	local UIStroke158 = Instance.new("UIStroke",TextButton155)
	UIStroke158["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke158["Thickness"] = 3
	UIStroke158["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke158----
	--Script159--
	local Script159 = Instance.new("Script",TextButton155)
	----Script159----
	--Script160--
	local Script160 = Instance.new("Script",TextButton155)
	Script160["Name"] = "ButtonHover"
	----Script160----
	--Frame161--
	local Frame161 = Instance.new("Frame",Frame2)
	Frame161["Visible"] = false
	Frame161["ClipsDescendants"] = true
	Frame161["Name"] = "Arsenal"
	Frame161["Position"] = UDim2.new(0.0159126688, 0, -0.000813337043, 0)
	Frame161["BorderColor3"] = Color3.new(0, 0, 0)
	Frame161["Size"] = UDim2.new(0, 630, 0, 349)
	Frame161["BorderSizePixel"] = 0
	Frame161["BackgroundColor3"] = Color3.new(0.0313726, 0.635294, 0.482353)
	----Frame161----
	--UICorner162--
	local UICorner162 = Instance.new("UICorner",Frame161)
	UICorner162["CornerRadius"] = UDim.new(0, 4)
	----UICorner162----
	--UIStroke163--
	local UIStroke163 = Instance.new("UIStroke",Frame161)
	UIStroke163["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke163["Thickness"] = 5
	----UIStroke163----
	--ScrollingFrame164--
	local ScrollingFrame164 = Instance.new("ScrollingFrame",Frame161)
	ScrollingFrame164["ScrollBarImageColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame164["Size"] = UDim2.new(0, 568, 0, 350)
	ScrollingFrame164["BackgroundTransparency"] = 1
	ScrollingFrame164["Active"] = true
	ScrollingFrame164["BorderColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame164["Position"] = UDim2.new(0.100000001, 0, 0, 0)
	ScrollingFrame164["BorderSizePixel"] = 0
	ScrollingFrame164["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ScrollingFrame164----
	--UICorner165--
	local UICorner165 = Instance.new("UICorner",ScrollingFrame164)
	UICorner165["CornerRadius"] = UDim.new(0, 4)
	----UICorner165----
	--UIListLayout166--
	local UIListLayout166 = Instance.new("UIListLayout",ScrollingFrame164)
	UIListLayout166["Padding"] = UDim.new(0, 16)
	UIListLayout166["HorizontalAlignment"] = Enum.HorizontalAlignment.Center
	UIListLayout166["SortOrder"] = Enum.SortOrder.LayoutOrder
	----UIListLayout166----
	--TextButton167--
	local TextButton167 = Instance.new("TextButton",ScrollingFrame164)
	TextButton167["TextWrapped"] = true
	TextButton167["TextColor3"] = Color3.new(1, 1, 1)
	TextButton167["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton167["Text"] = ""
	TextButton167["TextSize"] = 14
	TextButton167["TextStrokeTransparency"] = 0
	TextButton167["TextScaled"] = true
	TextButton167["Font"] = Enum.Font.FredokaOne
	TextButton167["BackgroundTransparency"] = 1
	TextButton167["Position"] = UDim2.new(0.0862676054, 0, 0, 0)
	TextButton167["Name"] = "Yellowgreg"
	TextButton167["Size"] = UDim2.new(0, 470, 0, 8)
	TextButton167["BorderSizePixel"] = 0
	TextButton167["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton167----
	--TextButton168--
	local TextButton168 = Instance.new("TextButton",ScrollingFrame164)
	TextButton168["TextWrapped"] = true
	TextButton168["TextColor3"] = Color3.new(1, 1, 1)
	TextButton168["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton168["Text"] = "CARTEL"
	TextButton168["TextStrokeTransparency"] = 0
	TextButton168["TextSize"] = 14
	TextButton168["Font"] = Enum.Font.FredokaOne
	TextButton168["Name"] = "CARTEL"
	TextButton168["Position"] = UDim2.new(-0.00968309864, 0, 0, 0)
	TextButton168["TextScaled"] = true
	TextButton168["Size"] = UDim2.new(0, 470, 0, 51)
	TextButton168["BorderSizePixel"] = 0
	TextButton168["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton168----
	--Script169--
	local Script169 = Instance.new("Script",TextButton168)
	Script169["Name"] = "Execute"
	----Script169----
	--UIStroke170--
	local UIStroke170 = Instance.new("UIStroke",TextButton168)
	UIStroke170["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke170["Thickness"] = 3
	UIStroke170["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke170----
	--Script171--
	local Script171 = Instance.new("Script",TextButton168)
	----Script171----
	--UICorner172--
	local UICorner172 = Instance.new("UICorner",TextButton168)
	UICorner172["CornerRadius"] = UDim.new(0, 4)
	----UICorner172----
	--Script173--
	local Script173 = Instance.new("Script",TextButton168)
	Script173["Name"] = "ButtonHover"
	----Script173----
	--TextButton174--
	local TextButton174 = Instance.new("TextButton",ScrollingFrame164)
	TextButton174["TextWrapped"] = true
	TextButton174["TextColor3"] = Color3.new(1, 1, 1)
	TextButton174["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton174["Text"] = "TOBIA"
	TextButton174["TextStrokeTransparency"] = 0
	TextButton174["TextSize"] = 14
	TextButton174["Font"] = Enum.Font.FredokaOne
	TextButton174["Name"] = "tobia"
	TextButton174["Position"] = UDim2.new(-0.00968309864, 0, 0, 0)
	TextButton174["TextScaled"] = true
	TextButton174["Size"] = UDim2.new(0, 470, 0, 51)
	TextButton174["BorderSizePixel"] = 0
	TextButton174["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton174----
	--Script175--
	local Script175 = Instance.new("Script",TextButton174)
	Script175["Name"] = "Execute"
	----Script175----
	--UIStroke176--
	local UIStroke176 = Instance.new("UIStroke",TextButton174)
	UIStroke176["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke176["Thickness"] = 3
	UIStroke176["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke176----
	--Script177--
	local Script177 = Instance.new("Script",TextButton174)
	----Script177----
	--UICorner178--
	local UICorner178 = Instance.new("UICorner",TextButton174)
	UICorner178["CornerRadius"] = UDim.new(0, 4)
	----UICorner178----
	--Script179--
	local Script179 = Instance.new("Script",TextButton174)
	Script179["Name"] = "ButtonHover"
	----Script179----
	--TextButton180--
	local TextButton180 = Instance.new("TextButton",ScrollingFrame164)
	TextButton180["TextWrapped"] = true
	TextButton180["TextColor3"] = Color3.new(1, 1, 1)
	TextButton180["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton180["Text"] = "HELL"
	TextButton180["TextStrokeTransparency"] = 0
	TextButton180["TextSize"] = 14
	TextButton180["Font"] = Enum.Font.FredokaOne
	TextButton180["Name"] = "HELL"
	TextButton180["Position"] = UDim2.new(0.112676054, 0, 0.451428562, 0)
	TextButton180["TextScaled"] = true
	TextButton180["Size"] = UDim2.new(0, 470, 0, 51)
	TextButton180["BorderSizePixel"] = 0
	TextButton180["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton180----
	--Script181--
	local Script181 = Instance.new("Script",TextButton180)
	Script181["Name"] = "Execute"
	----Script181----
	--UIStroke182--
	local UIStroke182 = Instance.new("UIStroke",TextButton180)
	UIStroke182["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke182["Thickness"] = 3
	UIStroke182["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke182----
	--Script183--
	local Script183 = Instance.new("Script",TextButton180)
	----Script183----
	--UICorner184--
	local UICorner184 = Instance.new("UICorner",TextButton180)
	UICorner184["CornerRadius"] = UDim.new(0, 4)
	----UICorner184----
	--Script185--
	local Script185 = Instance.new("Script",TextButton180)
	Script185["Name"] = "ButtonHover"
	----Script185----
	--Frame186--
	local Frame186 = Instance.new("Frame",Frame2)
	Frame186["Visible"] = false
	Frame186["ClipsDescendants"] = true
	Frame186["Name"] = "Real"
	Frame186["Position"] = UDim2.new(0.0159126688, 0, -0.000813337043, 0)
	Frame186["BorderColor3"] = Color3.new(0, 0, 0)
	Frame186["Size"] = UDim2.new(0, 630, 0, 349)
	Frame186["BorderSizePixel"] = 0
	Frame186["BackgroundColor3"] = Color3.new(0.0313726, 0.635294, 0.482353)
	----Frame186----
	--UICorner187--
	local UICorner187 = Instance.new("UICorner",Frame186)
	UICorner187["CornerRadius"] = UDim.new(0, 4)
	----UICorner187----
	--UIStroke188--
	local UIStroke188 = Instance.new("UIStroke",Frame186)
	UIStroke188["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke188["Thickness"] = 5
	----UIStroke188----
	--ScrollingFrame189--
	local ScrollingFrame189 = Instance.new("ScrollingFrame",Frame186)
	ScrollingFrame189["ScrollBarImageColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame189["Size"] = UDim2.new(0, 568, 0, 350)
	ScrollingFrame189["BackgroundTransparency"] = 1
	ScrollingFrame189["Active"] = true
	ScrollingFrame189["BorderColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame189["Position"] = UDim2.new(0.100000001, 0, 0, 0)
	ScrollingFrame189["BorderSizePixel"] = 0
	ScrollingFrame189["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ScrollingFrame189----
	--UICorner190--
	local UICorner190 = Instance.new("UICorner",ScrollingFrame189)
	UICorner190["CornerRadius"] = UDim.new(0, 4)
	----UICorner190----
	--UIListLayout191--
	local UIListLayout191 = Instance.new("UIListLayout",ScrollingFrame189)
	UIListLayout191["Padding"] = UDim.new(0, 16)
	UIListLayout191["HorizontalAlignment"] = Enum.HorizontalAlignment.Center
	UIListLayout191["SortOrder"] = Enum.SortOrder.LayoutOrder
	----UIListLayout191----
	--TextButton192--
	local TextButton192 = Instance.new("TextButton",ScrollingFrame189)
	TextButton192["TextWrapped"] = true
	TextButton192["TextColor3"] = Color3.new(1, 1, 1)
	TextButton192["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton192["Text"] = "Yellowgreg"
	TextButton192["TextStrokeTransparency"] = 0
	TextButton192["TextSize"] = 14
	TextButton192["Font"] = Enum.Font.FredokaOne
	TextButton192["Name"] = "Yellowgreg"
	TextButton192["Position"] = UDim2.new(-0.00968309864, 0, 0, 0)
	TextButton192["TextScaled"] = true
	TextButton192["Size"] = UDim2.new(0, 470, 0, 51)
	TextButton192["BorderSizePixel"] = 0
	TextButton192["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton192----
	--Script193--
	local Script193 = Instance.new("Script",TextButton192)
	Script193["Name"] = "Execute"
	----Script193----
	--UIStroke194--
	local UIStroke194 = Instance.new("UIStroke",TextButton192)
	UIStroke194["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke194["Thickness"] = 3
	UIStroke194["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke194----
	--Script195--
	local Script195 = Instance.new("Script",TextButton192)
	----Script195----
	--UICorner196--
	local UICorner196 = Instance.new("UICorner",TextButton192)
	UICorner196["CornerRadius"] = UDim.new(0, 4)
	----UICorner196----
	--Script197--
	local Script197 = Instance.new("Script",TextButton192)
	Script197["Name"] = "ButtonHover"
	----Script197----
	--TextButton198--
	local TextButton198 = Instance.new("TextButton",ScrollingFrame189)
	TextButton198["TextWrapped"] = true
	TextButton198["TextColor3"] = Color3.new(1, 1, 1)
	TextButton198["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton198["Text"] = ""
	TextButton198["TextSize"] = 14
	TextButton198["TextStrokeTransparency"] = 0
	TextButton198["TextScaled"] = true
	TextButton198["Font"] = Enum.Font.FredokaOne
	TextButton198["BackgroundTransparency"] = 1
	TextButton198["Position"] = UDim2.new(0.0862676054, 0, 0, 0)
	TextButton198["Name"] = "Yellowgreg"
	TextButton198["Size"] = UDim2.new(0, 470, 0, 8)
	TextButton198["BorderSizePixel"] = 0
	TextButton198["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton198----
	--Frame199--
	local Frame199 = Instance.new("Frame",Frame2)
	Frame199["Visible"] = false
	Frame199["ClipsDescendants"] = true
	Frame199["Name"] = "Rivals"
	Frame199["Position"] = UDim2.new(0.0159126688, 0, -0.000813337043, 0)
	Frame199["BorderColor3"] = Color3.new(0, 0, 0)
	Frame199["Size"] = UDim2.new(0, 630, 0, 349)
	Frame199["BorderSizePixel"] = 0
	Frame199["BackgroundColor3"] = Color3.new(0.0313726, 0.635294, 0.482353)
	----Frame199----
	--UICorner200--
	local UICorner200 = Instance.new("UICorner",Frame199)
	UICorner200["CornerRadius"] = UDim.new(0, 4)
	----UICorner200----
	--UIStroke201--
	local UIStroke201 = Instance.new("UIStroke",Frame199)
	UIStroke201["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke201["Thickness"] = 5
	----UIStroke201----
	--ScrollingFrame202--
	local ScrollingFrame202 = Instance.new("ScrollingFrame",Frame199)
	ScrollingFrame202["ScrollBarImageColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame202["Size"] = UDim2.new(0, 568, 0, 350)
	ScrollingFrame202["BackgroundTransparency"] = 1
	ScrollingFrame202["Active"] = true
	ScrollingFrame202["BorderColor3"] = Color3.new(0, 0, 0)
	ScrollingFrame202["Position"] = UDim2.new(0.100000001, 0, 0, 0)
	ScrollingFrame202["BorderSizePixel"] = 0
	ScrollingFrame202["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ScrollingFrame202----
	--UICorner203--
	local UICorner203 = Instance.new("UICorner",ScrollingFrame202)
	UICorner203["CornerRadius"] = UDim.new(0, 4)
	----UICorner203----
	--UIListLayout204--
	local UIListLayout204 = Instance.new("UIListLayout",ScrollingFrame202)
	UIListLayout204["Padding"] = UDim.new(0, 16)
	UIListLayout204["HorizontalAlignment"] = Enum.HorizontalAlignment.Center
	UIListLayout204["SortOrder"] = Enum.SortOrder.LayoutOrder
	----UIListLayout204----
	--TextButton205--
	local TextButton205 = Instance.new("TextButton",ScrollingFrame202)
	TextButton205["TextWrapped"] = true
	TextButton205["TextColor3"] = Color3.new(1, 1, 1)
	TextButton205["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton205["Text"] = ""
	TextButton205["TextSize"] = 14
	TextButton205["TextStrokeTransparency"] = 0
	TextButton205["TextScaled"] = true
	TextButton205["Font"] = Enum.Font.FredokaOne
	TextButton205["BackgroundTransparency"] = 1
	TextButton205["Position"] = UDim2.new(0.0862676054, 0, 0, 0)
	TextButton205["Name"] = "Yellowgreg"
	TextButton205["Size"] = UDim2.new(0, 470, 0, 8)
	TextButton205["BorderSizePixel"] = 0
	TextButton205["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton205----
	--TextButton206--
	local TextButton206 = Instance.new("TextButton",ScrollingFrame202)
	TextButton206["TextWrapped"] = true
	TextButton206["TextColor3"] = Color3.new(1, 1, 1)
	TextButton206["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton206["Text"] = "Actyrn"
	TextButton206["TextStrokeTransparency"] = 0
	TextButton206["TextSize"] = 14
	TextButton206["Font"] = Enum.Font.FredokaOne
	TextButton206["Name"] = "Actyrn"
	TextButton206["Position"] = UDim2.new(-0.00968309864, 0, 0, 0)
	TextButton206["TextScaled"] = true
	TextButton206["Size"] = UDim2.new(0, 470, 0, 51)
	TextButton206["BorderSizePixel"] = 0
	TextButton206["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton206----
	--Script207--
	local Script207 = Instance.new("Script",TextButton206)
	Script207["Name"] = "Execute"
	----Script207----
	--UIStroke208--
	local UIStroke208 = Instance.new("UIStroke",TextButton206)
	UIStroke208["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke208["Thickness"] = 3
	UIStroke208["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke208----
	--Script209--
	local Script209 = Instance.new("Script",TextButton206)
	----Script209----
	--UICorner210--
	local UICorner210 = Instance.new("UICorner",TextButton206)
	UICorner210["CornerRadius"] = UDim.new(0, 4)
	----UICorner210----
	--Script211--
	local Script211 = Instance.new("Script",TextButton206)
	Script211["Name"] = "ButtonHover"
	----Script211----
	--TextButton212--
	local TextButton212 = Instance.new("TextButton",ScrollingFrame202)
	TextButton212["TextWrapped"] = true
	TextButton212["TextColor3"] = Color3.new(1, 1, 1)
	TextButton212["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton212["Text"] = "CriShoux"
	TextButton212["TextStrokeTransparency"] = 0
	TextButton212["TextSize"] = 14
	TextButton212["Font"] = Enum.Font.FredokaOne
	TextButton212["Name"] = "CriShoux"
	TextButton212["Position"] = UDim2.new(-0.00968309864, 0, 0, 0)
	TextButton212["TextScaled"] = true
	TextButton212["Size"] = UDim2.new(0, 470, 0, 51)
	TextButton212["BorderSizePixel"] = 0
	TextButton212["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton212----
	--Script213--
	local Script213 = Instance.new("Script",TextButton212)
	Script213["Name"] = "Execute"
	----Script213----
	--UIStroke214--
	local UIStroke214 = Instance.new("UIStroke",TextButton212)
	UIStroke214["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke214["Thickness"] = 3
	UIStroke214["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke214----
	--Script215--
	local Script215 = Instance.new("Script",TextButton212)
	----Script215----
	--UICorner216--
	local UICorner216 = Instance.new("UICorner",TextButton212)
	UICorner216["CornerRadius"] = UDim.new(0, 4)
	----UICorner216----
	--Script217--
	local Script217 = Instance.new("Script",TextButton212)
	Script217["Name"] = "ButtonHover"
	----Script217----
	--TextButton218--
	local TextButton218 = Instance.new("TextButton",ScrollingFrame202)
	TextButton218["TextWrapped"] = true
	TextButton218["TextColor3"] = Color3.new(1, 1, 1)
	TextButton218["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton218["Text"] = "zeynwq"
	TextButton218["TextStrokeTransparency"] = 0
	TextButton218["TextSize"] = 14
	TextButton218["Font"] = Enum.Font.FredokaOne
	TextButton218["Name"] = "zeynwq"
	TextButton218["Position"] = UDim2.new(-0.00968309864, 0, 0, 0)
	TextButton218["TextScaled"] = true
	TextButton218["Size"] = UDim2.new(0, 470, 0, 51)
	TextButton218["BorderSizePixel"] = 0
	TextButton218["BackgroundColor3"] = Color3.new(0.647059, 0.819608, 0.835294)
	----TextButton218----
	--Script219--
	local Script219 = Instance.new("Script",TextButton218)
	Script219["Name"] = "Execute"
	----Script219----
	--UIStroke220--
	local UIStroke220 = Instance.new("UIStroke",TextButton218)
	UIStroke220["Color"] = Color3.new(0.301961, 0.301961, 0.301961)
	UIStroke220["Thickness"] = 3
	UIStroke220["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke220----
	--Script221--
	local Script221 = Instance.new("Script",TextButton218)
	----Script221----
	--UICorner222--
	local UICorner222 = Instance.new("UICorner",TextButton218)
	UICorner222["CornerRadius"] = UDim.new(0, 4)
	----UICorner222----
	--Script223--
	local Script223 = Instance.new("Script",TextButton218)
	Script223["Name"] = "ButtonHover"
	----Script223----
	--ImageButton224--
	local ImageButton224 = Instance.new("ImageButton",ScreenGui1)
	ImageButton224["HoverImage"] = "http://www.roblox.com/asset/?id=104152688956164"
	ImageButton224["BorderColor3"] = Color3.new(0, 0, 0)
	ImageButton224["Image"] = "http://www.roblox.com/asset/?id=104152688956164"
	ImageButton224["BackgroundTransparency"] = 1
	ImageButton224["Position"] = UDim2.new(0.204155907, 0, 0.22842373, 0)
	ImageButton224["Size"] = UDim2.new(0.0446456857, 0, 0.0759362504, 0)
	ImageButton224["ZIndex"] = 2
	ImageButton224["BorderSizePixel"] = 0
	ImageButton224["BackgroundColor3"] = Color3.new(0.286275, 0.545098, 0.596078)
	----ImageButton224----
	--Script225--
	local Script225 = Instance.new("Script",ImageButton224)
	----Script225----
	--ImageLabel226--
	local ImageLabel226 = Instance.new("ImageLabel",ImageButton224)
	ImageLabel226["ImageColor3"] = Color3.new(0.0666667, 1, 0)
	ImageLabel226["ImageTransparency"] = 1
	ImageLabel226["BorderColor3"] = Color3.new(0, 0, 0)
	ImageLabel226["Image"] = "http://www.roblox.com/asset/?id=73037027480437"
	ImageLabel226["BackgroundTransparency"] = 1
	ImageLabel226["Position"] = UDim2.new(0, 0, -0.536232114, 0)
	ImageLabel226["Size"] = UDim2.new(0, 75, 0, 37)
	ImageLabel226["BorderSizePixel"] = 0
	ImageLabel226["BackgroundColor3"] = Color3.new(1, 1, 1)
	----ImageLabel226----
	--Script227--
	local Script227 = Instance.new("Script",ImageButton224)
	Script227["Name"] = "OpenFrames"
	----Script227----
	--Script228--
	local Script228 = Instance.new("Script",ImageButton224)
	Script228["Name"] = "Drag"
	----Script228----
	--UIStroke229--
	local UIStroke229 = Instance.new("UIStroke",ImageButton224)
	UIStroke229["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke229["Thickness"] = 5
	----UIStroke229----
	--UICorner230--
	local UICorner230 = Instance.new("UICorner",ImageButton224)
	UICorner230["CornerRadius"] = UDim.new(1, 0)
	----UICorner230----
	--Script231--
	local Script231 = Instance.new("Script",ScreenGui1)
	----Script231----
	--Frame232--
	local Frame232 = Instance.new("Frame",ScreenGui1)
	Frame232["Size"] = UDim2.new(0, 1255, 0, 565)
	Frame232["Name"] = "advertisment"
	Frame232["Position"] = UDim2.new(0.188443571, 0, 0.153831244, 0)
	Frame232["BorderColor3"] = Color3.new(0, 0, 0)
	Frame232["ZIndex"] = 2
	Frame232["BorderSizePixel"] = 0
	Frame232["BackgroundColor3"] = Color3.new(0.0352941, 0.756863, 0.576471)
	----Frame232----
	--TextLabel233--
	local TextLabel233 = Instance.new("TextLabel",Frame232)
	TextLabel233["TextWrapped"] = true
	TextLabel233["TextColor3"] = Color3.new(0.470588, 0.972549, 1)
	TextLabel233["BorderColor3"] = Color3.new(0, 0, 0)
	TextLabel233["Text"] = "garry hub | advertisment"
	TextLabel233["TextStrokeTransparency"] = 0
	TextLabel233["Font"] = Enum.Font.SourceSans
	TextLabel233["TextSize"] = 14
	TextLabel233["Position"] = UDim2.new(0.101973847, 0, 0.118591875, 0)
	TextLabel233["TextScaled"] = true
	TextLabel233["Size"] = UDim2.new(0, 949, 0, 128)
	TextLabel233["BorderSizePixel"] = 0
	TextLabel233["BackgroundColor3"] = Color3.new(0.0509804, 0.462745, 0.470588)
	----TextLabel233----
	--UIStroke234--
	local UIStroke234 = Instance.new("UIStroke",TextLabel233)
	UIStroke234["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	UIStroke234["Thickness"] = 5
	----UIStroke234----
	--UICorner235--
	local UICorner235 = Instance.new("UICorner",TextLabel233)
	UICorner235["CornerRadius"] = UDim.new(0, 4)
	----UICorner235----
	--TextBox236--
	local TextBox236 = Instance.new("TextBox",Frame232)
	TextBox236["CursorPosition"] = -1
	TextBox236["TextWrapped"] = true
	TextBox236["ClearTextOnFocus"] = false
	TextBox236["TextColor3"] = Color3.new(0, 0, 0)
	TextBox236["BorderColor3"] = Color3.new(0, 0, 0)
	TextBox236["Text"] = ""
	TextBox236["Size"] = UDim2.new(0, 1109, 0, 89)
	TextBox236["TextStrokeTransparency"] = 0
	TextBox236["PlaceholderColor3"] = Color3.new(0, 1, 0.415686)
	TextBox236["Font"] = Enum.Font.SourceSans
	TextBox236["TextSize"] = 14
	TextBox236["Position"] = UDim2.new(0.0339912474, 0, 0.420454741, 0)
	TextBox236["TextScaled"] = true
	TextBox236["PlaceholderText"] = "https://discord.gg/avpmgtZsFr"
	TextBox236["BorderSizePixel"] = 0
	TextBox236["BackgroundColor3"] = Color3.new(0.0862745, 0.788235, 0.8)
	----TextBox236----
	--UIStroke237--
	local UIStroke237 = Instance.new("UIStroke",TextBox236)
	UIStroke237["Color"] = Color3.new(0, 1, 1)
	UIStroke237["Thickness"] = 5
	UIStroke237["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke237----
	--UICorner238--
	local UICorner238 = Instance.new("UICorner",TextBox236)
	UICorner238["CornerRadius"] = UDim.new(0, 4)
	----UICorner238----
	--TextButton239--
	local TextButton239 = Instance.new("TextButton",Frame232)
	TextButton239["TextWrapped"] = true
	TextButton239["TextColor3"] = Color3.new(0, 0.929412, 0.388235)
	TextButton239["BorderColor3"] = Color3.new(0, 0, 0)
	TextButton239["Text"] = "click to show the hub"
	TextButton239["TextStrokeTransparency"] = 0
	TextButton239["Font"] = Enum.Font.SourceSans
	TextButton239["TextSize"] = 14
	TextButton239["Position"] = UDim2.new(0.307995468, 0, 0.902607679, 0)
	TextButton239["TextScaled"] = true
	TextButton239["Size"] = UDim2.new(0, 457, 0, 92)
	TextButton239["BorderSizePixel"] = 0
	TextButton239["BackgroundColor3"] = Color3.new(0.0862745, 0.788235, 0.8)
	----TextButton239----
	--Script240--
	local Script240 = Instance.new("Script",TextButton239)
	----Script240----
	--UICorner241--
	local UICorner241 = Instance.new("UICorner",TextButton239)
	UICorner241["CornerRadius"] = UDim.new(0, 4)
	----UICorner241----
	--UIStroke242--
	local UIStroke242 = Instance.new("UIStroke",TextButton239)
	UIStroke242["Color"] = Color3.new(0, 1, 1)
	UIStroke242["Thickness"] = 5
	UIStroke242["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke242----
	--TextBox243--
	local TextBox243 = Instance.new("TextBox",Frame232)
	TextBox243["CursorPosition"] = -1
	TextBox243["TextWrapped"] = true
	TextBox243["ClearTextOnFocus"] = false
	TextBox243["TextColor3"] = Color3.new(0, 1, 0.14902)
	TextBox243["BorderColor3"] = Color3.new(0, 0, 0)
	TextBox243["Text"] = "join the discord for more updates!"
	TextBox243["Size"] = UDim2.new(0, 1109, 0, 74)
	TextBox243["TextStrokeTransparency"] = 0
	TextBox243["PlaceholderColor3"] = Color3.new(0, 1, 0.415686)
	TextBox243["Font"] = Enum.Font.SourceSans
	TextBox243["TextSize"] = 14
	TextBox243["Position"] = UDim2.new(0.0339912586, 0, 0.597363472, 0)
	TextBox243["TextScaled"] = true
	TextBox243["PlaceholderText"] = "join the discord for more updates!"
	TextBox243["BorderSizePixel"] = 0
	TextBox243["BackgroundColor3"] = Color3.new(0.0862745, 0.788235, 0.8)
	----TextBox243----
	--UICorner244--
	local UICorner244 = Instance.new("UICorner",TextBox243)
	UICorner244["CornerRadius"] = UDim.new(0, 4)
	----UICorner244----
	--UIStroke245--
	local UIStroke245 = Instance.new("UIStroke",TextBox243)
	UIStroke245["Color"] = Color3.new(0, 1, 1)
	UIStroke245["Thickness"] = 5
	UIStroke245["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke245----
	--UIStroke246--
	local UIStroke246 = Instance.new("UIStroke",Frame232)
	UIStroke246["Color"] = Color3.new(0, 1, 1)
	UIStroke246["Thickness"] = 5
	UIStroke246["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border
	----UIStroke246----
	spawn(function() --Source for Script7
		local script = Script7
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, -0.2, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.31, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script9
		local script = Script9
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, -0.1, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.05, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script11
		local script = Script11
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, -0.2, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.31, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script13
		local script = Script13
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, -0.1, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.4, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script15
		local script = Script15
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, -0.2, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.31, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script17
		local script = Script17
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, -0.1, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.05, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script19
		local script = Script19
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, -0.2, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.31, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script21
		local script = Script21
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, -0.15, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.1, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script24
		local script = Script24
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, 0.2, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.31, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script26
		local script = Script26
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, 0.2, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.31, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script28
		local script = Script28
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, 0.1, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.05, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script30
		local script = Script30
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, 0.05, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.1, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script32
		local script = Script32
		-- References
		local imageLabel = script.Parent -- Replace this with the path to your ImageLabel
		local tweenService = game:GetService("TweenService")
		
		-- Animation Settings
		local startPosition = imageLabel.Position -- Dynamically fetch the original position of the ImageLabel
		local upPosition = startPosition - UDim2.new(0, 0, 0.1, 0) -- Slightly higher (move up by 0.2 on Y axis)
		local downPosition = startPosition + UDim2.new(0, 0, 0.4, 0) -- Slightly lower (move down by 0.2 on Y axis)
		local tweenTime = 1 -- Time it takes to move up or down
		
		-- Tween Info
		local tweenInfo = TweenInfo.new(
			tweenTime, -- Time
			Enum.EasingStyle.Sine, -- Smooth and natural easing style
			Enum.EasingDirection.InOut, -- Eases in and out for smooth transitions
			-1, -- Repeat count (-1 means infinite)
			true -- Automatically reverse the tween
		)
		
		-- Create the Tween
		local tween = tweenService:Create(imageLabel, tweenInfo, {Position = upPosition})
		
		-- Start the Animation
		tween:Play()
		
	end)
	spawn(function() --Source for Script42
		local script = Script42
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script43
		local script = Script43
		-- Place this script inside each TextButton
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Home
		local parentGui = script.Parent.Parent
		
		-- Function to toggle the visibility of the target frame
		local function toggleTargetFrameVisibility()
			if targetFrame.Visible then
				targetFrame.Visible = false -- If it's visible, hide it
			else
				targetFrame.Visible = true -- If it's hidden, show it
			end
		end
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			toggleTargetFrameVisibility() -- Toggle the visibility on button click
		end)
		
	end)
	spawn(function() --Source for Script46
		local script = Script46
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script50
		local script = Script50
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script51
		local script = Script51
		-- Place this script inside each TextButton
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Fisch
		local parentGui = script.Parent.Parent
		
		-- Function to toggle the visibility of the target frame
		local function toggleTargetFrameVisibility()
			if targetFrame.Visible then
				targetFrame.Visible = false -- If it's visible, hide it
			else
				targetFrame.Visible = true -- If it's hidden, show it
			end
		end
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			toggleTargetFrameVisibility() -- Toggle the visibility on button click
		end)
		
	end)
	spawn(function() --Source for Script55
		local script = Script55
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script56
		local script = Script56
		-- Place this script inside each TextButton
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Real
		local parentGui = script.Parent.Parent
		
		-- Function to toggle the visibility of the target frame
		local function toggleTargetFrameVisibility()
			if targetFrame.Visible then
				targetFrame.Visible = false -- If it's visible, hide it
			else
				targetFrame.Visible = true -- If it's hidden, show it
			end
		end
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			toggleTargetFrameVisibility() -- Toggle the visibility on button click
		end)
		
	end)
	spawn(function() --Source for Script60
		local script = Script60
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script61
		local script = Script61
		-- Place this script inside each TextButton
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Arsenal
		local parentGui = script.Parent.Parent
		
		-- Function to toggle the visibility of the target frame
		local function toggleTargetFrameVisibility()
			if targetFrame.Visible then
				targetFrame.Visible = false -- If it's visible, hide it
			else
				targetFrame.Visible = true -- If it's hidden, show it
			end
		end
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			toggleTargetFrameVisibility() -- Toggle the visibility on button click
		end)
		
	end)
	spawn(function() --Source for Script65
		local script = Script65
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script66
		local script = Script66
		-- Place this script inside each TextButton
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Rivals
		local parentGui = script.Parent.Parent
		
		-- Function to toggle the visibility of the target frame
		local function toggleTargetFrameVisibility()
			if targetFrame.Visible then
				targetFrame.Visible = false -- If it's visible, hide it
			else
				targetFrame.Visible = true -- If it's hidden, show it
			end
		end
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			toggleTargetFrameVisibility() -- Toggle the visibility on button click
		end)
		
	end)
	spawn(function() --Source for Script69
		local script = Script69
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script73
		local script = Script73
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script76
		local script = Script76
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script82
		local script = Script82
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script84
		local script = Script84
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script88
		local script = Script88
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script90
		local script = Script90
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script92
		local script = Script92
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script94
		local script = Script94
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(11, 214, 160) -- Hover background color
		local hoverTextColor = Color3.fromRGB(4, 82, 83) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(138, 245, 255) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(9, 189, 189) -- Default background color
		local defaultTextColor = Color3.fromRGB(35, 251, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script102
		local script = Script102
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://naokihub.vercel.app",true))()
		end)
		
	end)
	spawn(function() --Source for Script103
		local script = Script103
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script106
		local script = Script106
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script108
		local script = Script108
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/kiciahook/kiciahook/refs/heads/main/loader.lua"))()
			end)
	end)
	spawn(function() --Source for Script109
		local script = Script109
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script112
		local script = Script112
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script116
		local script = Script116
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script117
		local script = Script117
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua", true))()
		end)
		
	end)
	spawn(function() --Source for Script118
		local script = Script118
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script120
		local script = Script120
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/Efe0626/RaitoHub/refs/heads/main/Script"))()
		end)
		
	end)
	spawn(function() --Source for Script123
		local script = Script123
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script124
		local script = Script124
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script126
		local script = Script126
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/londnee/code/refs/heads/main/Fisch.lua"))()
		end)
		
	end)
	spawn(function() --Source for Script129
		local script = Script129
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script130
		local script = Script130
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script132
		local script = Script132
		script.Parent.MouseButton1Click:Connect(function()
			(loadstring or load)(game:HttpGet("https://raw.githubusercontent.com/Native-lab/Native/main/loader.lua"))()
		end)
		
	end)
	spawn(function() --Source for Script135
		local script = Script135
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script136
		local script = Script136
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script138
		local script = Script138
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/Just3itx/Lunor-Loadstrings/refs/heads/main/Loader"))()
		end)
		
	end)
	spawn(function() --Source for Script141
		local script = Script141
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script142
		local script = Script142
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script144
		local script = Script144
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/kiciahook/kiciahook/refs/heads/main/loader.lua"))()
			end)
	end)
	spawn(function() --Source for Script147
		local script = Script147
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.HomeFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script148
		local script = Script148
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script150
		local script = Script150
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/3345-c-a-t-s-u-s/Kncrypt/refs/heads/main/sources/Fisch.lua"))({true})
		end)
		
	end)
	spawn(function() --Source for Script153
		local script = Script153
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script154
		local script = Script154
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script156
		local script = Script156
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://gist.githubusercontent.com/AverageHub/1980eccce4133d77fb24d166dc296125/raw/2d9c88acc21a302d92aed0e8b6f0dcd287c8b96b/gistfile1.txt"))()
		end)
		
	end)
	spawn(function() --Source for Script159
		local script = Script159
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 3 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(2) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script160
		local script = Script160
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script169
		local script = Script169
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/JackyPoopoo/cartel/main/0000000000000000000000000000000000000000000000000"))()
		end)
		
	end)
	spawn(function() --Source for Script171
		local script = Script171
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script173
		local script = Script173
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script175
		local script = Script175
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet('https://raw.githubusercontent.com/tobi437a/Scripts/main/958f8e21b7d85c61ef5456e6.lua'))()
		end)
		
	end)
	spawn(function() --Source for Script177
		local script = Script177
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script179
		local script = Script179
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script181
		local script = Script181
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/helldevelopment/RobloxScripts/main/SanityLoader.lua"))()
		end)
		
	end)
	spawn(function() --Source for Script183
		local script = Script183
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script185
		local script = Script185
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script193
		local script = Script193
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/YellowGregs/Loadstring/refs/heads/main/rhtestesting.lua"))()
		end)
		
	end)
	spawn(function() --Source for Script195
		local script = Script195
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script197
		local script = Script197
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script207
		local script = Script207
		script.Parent.MouseButton1Click:Connect(function()
		 loadstring(game:HttpGet("https://raw.githubusercontent.com/Actyrn/Scripts/main/AzureModded"))()
		end)
		
	end)
	spawn(function() --Source for Script209
		local script = Script209
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script211
		local script = Script211
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script213
		local script = Script213
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/CriShoux/OwlHub/master/OwlHub.txt"))();
		end)
		
	end)
	spawn(function() --Source for Script215
		local script = Script215
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script217
		local script = Script217
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script219
		local script = Script219
		script.Parent.MouseButton1Click:Connect(function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/zeynwq/main/main/rivals1"))()() 
		end)
		
	end)
	spawn(function() --Source for Script221
		local script = Script221
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.Parent.Parent.Parent.EclipticFrame
		local textLabel = targetFrame and targetFrame:FindFirstChild("ShowExecute") -- Reference to the TextLabel
		local TweenService = game:GetService("TweenService")
		
		-- Ensure EclipticFrame and ShowExecute exist
		if not targetFrame or not textLabel then
			warn("EclipticFrame or ShowExecute not found!")
			return
		end
		
		-- Tween settings
		local fadeDuration = 1 -- Duration for fade in/out
		local tweenInfo = TweenInfo.new(
			fadeDuration, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			-- Update the text
			textLabel.Text = "Successfully Executed " .. button.Name .. ", may take a while!"
		
			-- Ensure the TextLabel starts fully invisible
			textLabel.TextTransparency = 1
			textLabel.Visible = true
		
			-- Create fade-in tween
			local fadeInTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 0 })
		
			-- Create fade-out tween
			local fadeOutTween = TweenService:Create(textLabel, tweenInfo, { TextTransparency = 1 })
		
			-- Play the fade-in tween
			fadeInTween:Play()
		
			-- Fade out after fade-in completes
			fadeInTween.Completed:Connect(function()
				task.wait(1) -- Optional delay before fading out
				fadeOutTween:Play()
		
				-- Hide the TextLabel after fade-out
				fadeOutTween.Completed:Connect(function()
					textLabel.Visible = false
				end)
			end)
		end)
		
	end)
	spawn(function() --Source for Script223
		local script = Script223
		local button = script.Parent -- Replace this with the reference to your button
		local TweenService = game:GetService("TweenService")
		
		-- Tween settings
		local tweenInfo = TweenInfo.new(
			0.2, -- Duration (in seconds)
			Enum.EasingStyle.Quad, -- Easing style
			Enum.EasingDirection.Out -- Easing direction
		)
		
		-- Colors
		local hoverBackgroundColor = Color3.fromRGB(72, 152, 116) -- Hover background color
		local hoverTextColor = Color3.fromRGB(57, 55, 122) -- Hover text color
		local hoverTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Hover text stroke color
		local defaultBackgroundColor = Color3.fromRGB(165, 209, 213) -- Default background color
		local defaultTextColor = Color3.fromRGB(255, 255, 255) -- Default text color
		local defaultTextStrokeColor = Color3.fromRGB(0, 0, 0) -- Default text stroke color
		
		-- Hover (MouseEnter) Event
		button.MouseEnter:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = hoverBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = hoverTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = hoverTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
		-- MouseLeave Event
		button.MouseLeave:Connect(function()
			local backgroundTween = TweenService:Create(button, tweenInfo, { BackgroundColor3 = defaultBackgroundColor })
			local textTween = TweenService:Create(button, tweenInfo, { TextColor3 = defaultTextColor })
			local textStrokeTween = TweenService:Create(button, tweenInfo, { TextStrokeColor3 = defaultTextStrokeColor })
		
			backgroundTween:Play()
			textTween:Play()
			textStrokeTween:Play()
		end)
		
	end)
	spawn(function() --Source for Script225
		local script = Script225
		local TweenService = game:GetService("TweenService")
		
		local imageButton = script.Parent
		local originalSize = imageButton.Size
		local shrinkSize = originalSize - UDim2.new(0, 10, 0, 10) -- Adjust shrink amount
		local shrinkTime = 0.1 -- Time for shrinking
		local swingTimes = { 0.1, 0.2, 0.3 } -- Swing times for quick to slow stopping
		local angles = { 50, -40, 25, 0 } -- Swing angles for dramatic effect
		
		-- Function to shrink the button
		local function shrinkButton()
			local shrinkTween = TweenService:Create(imageButton, TweenInfo.new(shrinkTime, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), { Size = shrinkSize })
			shrinkTween:Play()
		end
		
		-- Function to resize and swing quickly, then slow down
		local function swingAndResizeButton()
			-- Resize back to original size
			local resizeTween = TweenService:Create(imageButton, TweenInfo.new(shrinkTime, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), { Size = originalSize })
			resizeTween:Play()
		
			-- Begin swinging after resizing
			resizeTween.Completed:Connect(function()
				local function swingTween(angle, time)
					return TweenService:Create(imageButton, TweenInfo.new(time, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), { Rotation = angle })
				end
		
				-- Perform quick swings, then slow down
				local swing1 = swingTween(angles[1], swingTimes[1])
				swing1:Play()
		
				swing1.Completed:Connect(function()
					local swing2 = swingTween(angles[2], swingTimes[2])
					swing2:Play()
		
					swing2.Completed:Connect(function()
						local swing3 = swingTween(angles[3], swingTimes[3])
						swing3:Play()
		
						swing3.Completed:Connect(function()
							-- Final settle
							local settleTween = swingTween(angles[4], swingTimes[3] / 2)
							settleTween:Play()
						end)
					end)
				end)
			end)
		end
		
		-- Connect events
		imageButton.MouseButton1Down:Connect(shrinkButton)
		imageButton.MouseButton1Up:Connect(swingAndResizeButton)
		
	end)
	spawn(function() --Source for Script227
		local script = Script227
		-- Place this script inside each TextButton
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Frame
		local parentGui = script.Parent.Parent
		
		-- Function to toggle the visibility of the target frame
		local function toggleTargetFrameVisibility()
			if targetFrame.Visible then
				targetFrame.Visible = false -- If it's visible, hide it
			else
				targetFrame.Visible = true -- If it's hidden, show it
			end
		end
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			toggleTargetFrameVisibility() -- Toggle the visibility on button click
		end)
		
	end)
	spawn(function() --Source for Script228
		local script = Script228
		local UserInputService = game:GetService("UserInputService")
		
		local gui = script.Parent
		
		local dragging
		local dragInput
		local dragStart
		local startPos
		
		local function update(input)
			local delta = input.Position - dragStart
			gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
		
		gui.InputBegan:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
				dragging = true
				dragStart = input.Position
				startPos = gui.Position
				
				input.Changed:Connect(function()
					if input.UserInputState == Enum.UserInputState.End then
						dragging = false
					end
				end)
			end
		end)
		
		gui.InputChanged:Connect(function(input)
			if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
				dragInput = input
			end
		end)
		
		UserInputService.InputChanged:Connect(function(input)
			if input == dragInput and dragging then
				update(input)
			end
		end)
	end)
	spawn(function() --Source for Script231
		local script = Script231
		local StarterGui = game:GetService("StarterGui")
		local notificationIcon = "http://www.roblox.com/asset/?id=122603982979089" -- Icon of the notification (optional)
		local eclipticFrame = script.Parent:FindFirstChild("Frame") -- Reference to the frame
		
		-- Ensure the EclipticFrame exists
		if not eclipticFrame then
			warn("EclipticFrame not found!")
			return
		end
		
		-- 
		-- Show the frame after the load time
		eclipticFrame.Visible = true
		
		--  the notification message with higher precision
		StarterGui:SetCore("SendNotification", {
			Title = "garry hub loaded",
			Icon = notificationIcon,
			Text = "join the discord!",
			Duration = 5, -- Notification duration in seconds
		})
		
	end)
	spawn(function() --Source for Script240
		local script = Script240
		-- Place this script inside each TextButton
		local button = script.Parent
		local targetFrame = script.Parent.Parent.Parent.advertisment
		local parentGui = script.Parent.Parent
		
		-- Function to toggle the visibility of the target frame
		local function toggleTargetFrameVisibility()
			if targetFrame.Visible then
				targetFrame.Visible = false -- If it's visible, hide it
			else
				targetFrame.Visible = true -- If it's hidden, show it
			end
		end
		
		-- Button click event
		button.MouseButton1Click:Connect(function()
			toggleTargetFrameVisibility() -- Toggle the visibility on button click
		end)
		
	end)
end
createGui(game.CoreGui) --coregui by default you should but change it
